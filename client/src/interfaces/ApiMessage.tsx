// Typescript interface for the ApiMessage  the obiect is the message from the API
// used to typecheck objects that are returned from the API and turns it to a string
export interface ApiMessage {
  message: string;
}
